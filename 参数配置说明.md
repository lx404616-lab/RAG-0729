# 模型与参数配置说明

本文说明本项目为何采用当前参数（见 `config.py` / `rag/indexer.py`），便于评审与复现调参。数值面向本仓库 `知识库/`（约 8 份中文 Markdown、产品手册类、多文档存在重复套话）标定，换库后建议重测拒答阈值与 Top-K。

---

## 1. 文档切分

| 参数 | 值 | 为什么这样配 |
|---|---|---|
| `CHUNK_SIZE` | `480` | 中文手册常见小节偏短；480 字可覆盖「标题路径 + 一段完整说明」，又不会把无关条款拼进同一向量，降低噪声。过大易混入多主题，过小则 SLA、价格等关键句被截断。 |
| `CHUNK_OVERLAP` | `100` | 滑动窗口兜底时保留约 1/5 重叠，避免跨窗切断「版本 + 指标」这类相邻信息；重叠过大则索引冗余、检索重复。 |
| `CHUNK_SOFT_EXTEND` | `100` | 在硬上限附近优先凑到句号/分号，减少半截句进入 Embedding；软扩展控制在 100，避免块无限变长。 |

切分策略本身按 Markdown 标题栈切段并注入标题路径：产品文档里「企业版 / SLA」等关键信息常在标题或路径中，路径注入能显著提升按版本/章节提问时的召回。

---

## 2. 检索

| 参数 | 值 | 为什么这样配 |
|---|---|---|
| `TOP_K` | `5` | 知识库规模小（百余块级），取 Top-5 兼顾覆盖与噪声；与引用对照、引用文档列表一一对应展示。 |
| `SCORE_THRESHOLD` | `0.52` | 在 BGE 余弦分上对照可答题与库外题标定。过往用 0.55 时，概括类问法（如「是什么产品」）在带/不带问号时分数会贴线误拒（约 0.54），故降至 0.52；库外题（天气、创始人等）仍多低于此线。 |

拒答文案固定为：「根据现有知识库，我无法回答该问题。」满足任务「无答案时明确说明」的要求。

---

## 3. 向量模型（BGE）

| 参数 | 值 | 为什么这样配 |
|---|---|---|
| `BGE_MODEL_NAME` | `BAAI/bge-small-zh-v1.5` | 中文检索专用、体量小（便于本机/笔试环境）、无需 GPU 也能建索引；相对字符 TF-IDF，对「年度可用性 / SLA」等同义与改写更稳。 |
| `BGE_QUERY_INSTRUCTION` | 官方中文 retrieval 前缀 | BGE 非对称检索要求：查询侧加 instruction、文档侧不加，否则分布错位、分数整体偏移。 |
| `BGE_BATCH_SIZE` | `32` | 建索引时吞吐与显存/内存的折中；本库规模下 32 足够快，无需再大。 |
| `BGE_LOCAL_FILES_ONLY` / HF 离线 | 默认优先本地缓存 | 避免反复访问 `huggingface.co` 超时；无缓存时可走 `HF_ENDPOINT=https://hf-mirror.com` 镜像下载。 |

### 检索打分为何「内容 + 路径 + 事实句」

本库多篇文档含相同产品介绍套话，单靠正文向量易被重复段落「冲淡」真正差异（如具体 SLA 数字）。因此：

- **内容向量（权重 0.55）**：主语义匹配；
- **标题路径向量（权重 0.45）**：强化「企业版 / 注意事项」等结构线索；
- **事实句单独编码再 max-pool**：关键数字、条款不被长段平均稀释。

最终分：`max(0.55·内容 + 0.45·路径, 事实句 max-pool)`。权重略偏正文，但给路径足够话语权；事实句用 max 而不是平均，是为了「命中一句关键句即可抬高该块」。

---

## 4. 生成模型（DeepSeek）

| 参数 | 值 | 为什么这样配 |
|---|---|---|
| `LLM_MODEL` | `deepseek-v4-flash` | 延迟与成本适合演示；OpenAI 兼容接口，接入简单；配合规定 Prompt 做引用标注与拒答。 |
| `LLM_BASE_URL` | `https://api.deepseek.com` | 官方兼容端点。 |
| `LLM_TEMPERATURE` | `0.2` | 知识库问答偏事实抽取，低温度减少发挥与改写；过低（0）有时过死板，0.2 兼顾可读性。 |
| `LLM_MAX_TOKENS` | `800` | Prompt 要求约 300 字内回答，800 为输出上限留余量（含引用标注），避免截断半句。 |
| `LLM_REASONING_EFFORT` | `high` | 多引用对照、矛盾说明时需要更稳的推理；本任务体量下成本可接受。 |
| `LLM_ENABLE_THINKING` | `True` | 与 DeepSeek 接口的 thinking 能力对齐，便于在复杂检索结果上做取舍（最终仍只展示正式回答）。 |
| `DEFAULT_ANSWER_MODE` | `ai` | 任务主路径是「基于检索生成回答」；无 Key 或仅本地时用 `--mode extractive`。 |

生成式必须配置 `DEEPSEEK_API_KEY`；抽取式不依赖大模型，仅做要点整理与 `[引用N]`，适合离线与对比演示。

---

## 5. 调参建议（换知识库时）

1. **先看召回**：相关问题 Top-1～Top-4 是否含正确段；不对则调切分/路径注入/事实句，而不是先拧温度。
2. **再定拒答线**：用 10～20 个库内问 + 若干库外问扫 `SCORE_THRESHOLD`，找「误拒」与「误答」平衡点。
3. **最后调生成**：幻觉多 → 降温度、收紧 Prompt、减小 `TOP_K`；答不全 → 略增 `TOP_K` 或 `CHUNK_SIZE`。

---

## 6. 与 README 的关系

- `README.md`：环境、运行命令、参数一览表（「是什么」）。
- 本文件：参数选型动机与标定背景（「为什么」）。

参数以 `config.py` 为准；检索权重在 `rag/indexer.py` 的 `VectorIndex` 中定义。
